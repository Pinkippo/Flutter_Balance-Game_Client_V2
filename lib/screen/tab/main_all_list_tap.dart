import 'package:flutter/material.dart';

class MainAllListTap extends StatelessWidget {
  const MainAllListTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('전체 리스트 탭'),
    );
  }
}
